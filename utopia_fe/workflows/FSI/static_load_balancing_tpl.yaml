app: stk_transfer
from:
  mesh: 
    type: file
    path: ${FLUID_MESH}
to:
  mesh: 
    type: file
    path: ${SOLID_MESH}

output_path: indicator.e
export_operator_imbalance: true
rescale_imbalance: ${RESCALE_IMBALANCE}
# 